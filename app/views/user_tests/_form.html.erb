<%= form_with(model: user_test, local: true) do |form| %>
  <% if user_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_test.errors.count, "error") %> prohibited this user_test from being saved:</h2>

      <ul>
        <% user_test.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :login %>
    <%= form.text_field :login %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
